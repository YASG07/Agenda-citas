const { appointmentData } = require('../data/appointment.data');

const appointmentResolver = {
    Query: {
        
    },
    Mutation: {
        
    }
}

module.exports = {
    appointmentResolver
};
